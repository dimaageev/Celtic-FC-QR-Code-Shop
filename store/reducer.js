import { KITS } from "../data/dummyData";

const initialState = {
  products: KITS,
};

const productsReducer = (state = initialState, action) => {
  return state;
};

export default productsReducer;
