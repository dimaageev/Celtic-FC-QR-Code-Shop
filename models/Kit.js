class Kit {
  constructor(id, title, imageUri, price, description) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.price = price;
    this.description = description;
  }
}

export default Kit;
